
import AccountNav from '@/components/AccountNav';


export default function AccountLayout({ children }) {
  return (
    
      <div>
        <AccountNav/>
        
        {children}</div>
  
  );
}